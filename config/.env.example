# OvernightPredict Configuration Example
# Copy this file to .env and fill in your values

# =============================================================================
# LLM Providers
# =============================================================================

# OpenAI Configuration
OPENAI_API_KEY=sk-your-openai-key-here
OPENAI_ENABLED=true
OPENAI_MODEL=gpt-4-turbo-preview
OPENAI_ORGANIZATION_ID=

# DeepSeek Configuration
DEEPSEEK_API_KEY=your-deepseek-key-here
DEEPSEEK_ENABLED=false
DEEPSEEK_MODEL=deepseek-coder

# Claude/Anthropic Configuration
CLAUDE_ENABLED=true
CLAUDE_MODEL=claude-3-opus-20240229
CLAUDE_RATE_LIMIT_WAIT=true

# Claude Authentication Type: api_key, oauth, session_key
CLAUDE_AUTH_TYPE=api_key

# Option 1: API Key Authentication (Anthropic API)
# Standard API key from Anthropic console
CLAUDE_API_KEY=sk-ant-your-anthropic-key-here

# Option 2: OAuth Authentication (Claude Pro/Team subscription)
# Uses browser-based login - no key needed, just set auth_type to oauth
# CLAUDE_AUTH_TYPE=oauth
# CLAUDE_OAUTH_CALLBACK_PORT=8080

# Option 3: Session Key Authentication (from browser cookies)
# Extract from browser after logging into claude.ai
# CLAUDE_AUTH_TYPE=session_key
# CLAUDE_SESSION_KEY=your-session-key-from-browser

# Subscription account info (for display, auto-detected with oauth/session_key)
# CLAUDE_ACCOUNT_EMAIL=your-email@example.com
# CLAUDE_SUBSCRIPTION_TYPE=pro  # free, pro, team, enterprise
# CLAUDE_DAILY_MESSAGE_LIMIT=100  # Leave empty for unlimited (API)

# =============================================================================
# Context Sharing (Optional)
# =============================================================================

# Enable context sharing across sessions
CONTEXT_ENABLED=false
CONTEXT_SHARING_TYPE=file  # file, cloud_bucket, redis

# File-based sharing
CONTEXT_SHARED_PATH=.overnight/shared

# Cloud bucket sharing (S3)
# CONTEXT_BUCKET_NAME=my-bucket
# CONTEXT_BUCKET_PREFIX=overnight-predict/
# AWS_REGION=us-east-1

# Redis sharing
# CONTEXT_REDIS_URL=redis://localhost:6379

# =============================================================================
# Prediction Configuration
# =============================================================================

# Accuracy threshold for considering a prediction correct
PREDICTION_ACCURACY_THRESHOLD=0.7

# Number of questions to predict ahead
PREDICTION_LOOKAHEAD_COUNT=5

# Minimum accuracy before strategy switch
PREDICTION_MIN_ACCURACY_FOR_KEEP=0.6

# Evaluation window size
PREDICTION_EVALUATION_WINDOW=10

# Semantic similarity model
PREDICTION_SIMILARITY_MODEL=all-MiniLM-L6-v2

# =============================================================================
# Orchestrator Configuration
# =============================================================================

# Maximum concurrent sessions
ORCHESTRATOR_MAX_SESSIONS=10

# Session timeout in seconds
ORCHESTRATOR_SESSION_TIMEOUT=3600

# Health check interval in seconds
ORCHESTRATOR_HEALTH_CHECK_INTERVAL=30

# Auto-recovery on failure
ORCHESTRATOR_AUTO_RECOVERY=true

# =============================================================================
# Application Settings
# =============================================================================

# Application name
APP_NAME=OvernightPredict

# Debug mode
DEBUG=false

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Working directory for storage
WORK_DIR=.overnight
